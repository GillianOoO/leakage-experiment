clear;

%%%for Example 1.
%Nq = 4;
%ErrVal = load('ErrVal.txt');%generated by 'BitDamp_gen_noise.m'
%ErrVal = ErrVal(:, 1: Nq);

%%%for Example 2.
Nq = 3;
ErrVal = load('p_randp.txt');%generated by 'BitDamp_gen_noise.m'


L = sum(ErrVal(1,:))/2^Nq;

S = sum(ErrVal(2,:))/(3^Nq-2^Nq);

fprintf('%e, %e\n', L, S);
